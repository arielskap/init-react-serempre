import styled from 'styled-components';

const Title = styled.section.attrs({
	className: "Title"
})`
	font-size: 1.5em;
	text-align: center;
	color: palevioletred;
`;

const Styles = {
	Title
}

export default Styles